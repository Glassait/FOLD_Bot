on:
  push:
    branches:
      - main
name: Update package version
jobs:
  release:
    name: Update version with release (1.0.0-x)
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.event.head_commit.message, 'release') }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18.X'
      - name: Setup Git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
      - name: Prerelease version
        run: npm version prerelease

  patch:
    name: Update version with patch (1.0.x-0)
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.event.head_commit.message, 'patch') }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18.X'
      - name: Setup Git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
      - name: Patch version
        run: npm version prepatch

  minor:
    name: Update version with minor (1.x.0-0)
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.event.head_commit.message, 'minor') }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18.X'
      - name: Setup Git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
      - name: Minor version
        run: npm version preminor

  major:
    name: Update version with major (x.0.0-0)
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.event.head_commit.message, 'major') }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18.X'
      - name: Setup Git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
      - name: Major version
        run: npm version premajor

  push:
    name: Push the version
    runs-on: ubuntu-latest
    needs:
      - release
      - patch
      - minor
      - major
    if: |
      needs.release.result == 'success' ||
      needs.patch.result == 'success' ||
      needs.minor.result == 'success' ||
      needs.major.result == 'success'
    env:
      GIT_PUSH_URL: https://${{ github.actor }}@github.com/${{ github.repository }}
    steps:
      - name: Setup Git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
      - name: Push version
        run: git push --tags $GIT_PUSH_URL main
